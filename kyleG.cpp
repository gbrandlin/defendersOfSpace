// Author: Kyle Gregory
// Date-modified: 2/22/18

// SHIP - check for collision with window edges
/*void ship_boundary () {
	if (g.ship.pos[0] < 0.0) {
		g.ship.pos[0] = 0.0;
	}
	else if (g.ship.pos[0] > (float)gl.xres) {
		g.ship.pos[0] = (float)gl.xres;
	}
	else if (g.ship.pos[1] < 0.0) {
		g.ship.pos[1] = 0.0;
	}
	else if (g.ship.pos[1] > (float)gl.yres) {
		g.ship.pos[1] = (float)gl.yres;
	}
}
// BULLET - check/delete for collision with window edges
void bullet_boundary () {
	if (b->pos[0] < 0.0) {
		memcpy(&g.barr[i], &g.barr[g.nbullets-1], sizeof(Bullet));
		g.nbullets--;
	}
	else if (b->pos[0] > (float)gl.xres) {
		memcpy(&g.barr[i], &g.barr[g.nbullets-1], sizeof(Bullet));
		g.nbullets--;
	}
	else if (b->pos[1] < 0.0) {
		memcpy(&g.barr[i], &g.barr[g.nbullets-1], sizeof(Bullet));
		g.nbullets--;
	}
	else if (b->pos[1] > (float)gl.yres) {
		memcpy(&g.barr[i], &g.barr[g.nbullets-1], sizeof(Bullet));
		g.nbullets--;
	}
}*/
